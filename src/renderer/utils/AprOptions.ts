// Add this interface to TimelineRenderer.ts or a separate type file
export interface AprOptions {
    aprMode?: boolean;
    progressPercent?: number;
    bookTitle?: string;
    authorUrl?: string;
}
