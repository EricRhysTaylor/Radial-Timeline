% Generated from DesignedStyleSpec v2 — bundled-fiction-contemporary-literary
% Do not edit by hand. Source of truth: src/publishing/designedStyle.ts
% Archetype: reading-draft

\documentclass[11pt,twoside]{book}

\usepackage{fontspec}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{etoolbox}
\usepackage{emptypage}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}
}

\geometry{
  paperwidth=6in,
  paperheight=9in,
  top=0.9in,
  bottom=1in,
  left=0.9in,
  right=0.9in
}

\defaultfontfeatures{Ligatures=TeX}
\setmainfont{Source Serif 4}[
  Path = /Users/ericrhystaylor/Documents/RT LLC/CodeBase/radial-timeline/src/assets/fonts/source-serif-4/ ,
  UprightFont = SourceSerif4-Regular.otf ,
  ItalicFont = SourceSerif4-It.otf ,
  BoldFont = SourceSerif4-Bold.otf ,
  BoldItalicFont = SourceSerif4-BoldIt.otf
]



\newcommand{\BookTitle}{Audit Book}
\newcommand{\AuthorName}{Audit Author}
\newcommand{\rtSceneRunningTitle}{}
\newcommand{\rtSetSceneRunningTitle}[1]{\gdef\rtSceneRunningTitle{#1}\markboth{\BookTitle}{#1}}
\fancypagestyle{rtEmpty}{\fancyhf{}\renewcommand{\headrulewidth}{0pt}\renewcommand{\footrulewidth}{0pt}}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\fancyhead[LE]{\normalfont\footnotesize\nouppercase{\BookTitle}}
\fancyhead[RO]{\normalfont\footnotesize\nouppercase{\rtSceneRunningTitle}}
\pagestyle{fancy}

\fancyfoot[C]{\thepage}

\pagenumbering{roman}
\newif\ifrtMainStarted
\rtMainStartedfalse
\newcommand{\rtBeginMainArabic}{%
  \clearpage
  \pagenumbering{arabic}%
  \setcounter{page}{1}%
  \rtMainStartedtrue%
}

\newcommand{\rtChapter}[2]{%
  \ifrtMainStarted\else\rtBeginMainArabic\fi%
  \clearpage
  \refstepcounter{chapter}%
  \null%
  \thispagestyle{rtEmpty}%
  \vspace*{0.46\textheight}%
  \begin{center}
    {\normalfont\bfseries\large Chapter~#1}\par
  \end{center}
  \vspace*{0.08\textheight}%
  \clearpage
}

\setcounter{secnumdepth}{0}
\makeatletter
\newcommand{\rtSceneOpenerTitle}[1]{%
  \begingroup
  \def\rt@sceneFirst{}%
  \rt@sceneFirstWord#1 \@nil
  \rt@sceneFirst
  \endgroup
}
\def\rt@sceneFirstWord#1 #2\@nil{\def\rt@sceneFirst{#1}}
\makeatother
\newcommand{\rtSceneSep}{%
  \clearpage
  \thispagestyle{empty}%
  \vspace*{2in}%
  \begin{center}{\Large * * *}\end{center}
  \vspace*{1in}%
}
\newcommand{\rtSceneOpener}[1]{%
  \ifrtMainStarted\else\rtBeginMainArabic\fi%
  \clearpage
  \null%
  \thispagestyle{empty}%
  \vspace*{0.16\textheight}%
  \begin{center}{\normalfont\bfseries\Large \rtSceneOpenerTitle{#1}}\end{center}
  \vspace*{0.12\textheight}%
}

\onehalfspacing
\usepackage{microtype}
\setlength{\emergencystretch}{3em}
\tolerance=1000
\hyphenpenalty=200
\setlength{\parindent}{1.5em}

\begin{document}

\rtChapter{1}{Boy with a Skull}

\rtSceneOpener{1}
\rtSetSceneRunningTitle{Arrival}

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

The first scene continues across pages so the PDF text audit can verify
the right-page running header. The prose itself avoids the scene title,
which means an extracted title hit must come from the header or opener
chrome.

\rtSceneOpener{2}
\rtSetSceneRunningTitle{The Garden}

Second scene body text follows the opener.

\rtChapter{2}{New Horizons}

\rtSceneOpener{3}
\rtSetSceneRunningTitle{Departure}

Third scene body text starts the second chapter.

\end{document}